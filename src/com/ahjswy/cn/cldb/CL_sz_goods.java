package com.ahjswy.cn.cldb;

import com.ahjswy.cn.cldb.bean.sz_goods;
import com.ahjswy.cn.dao.DBOpenHelper;

import android.content.ContentValues;
import android.database.sqlite.SQLiteDatabase;

public class CL_sz_goods extends CloudDBBase {
	// SELECT MAX(id)+1 FROM sz_goods 获取 最后一条id 记录 +1
	public void inset(sz_goods good) {
		// String sql = "";
		// executeQuery(sql);
		// DBOpenHelper helper = new DBOpenHelper();
		// SQLiteDatabase db = helper.getWritableDatabase();
		// ContentValues values = new ContentValues();
		// values.size();
	}

}
